name: nba_shot_analytics
version: "1.0.0"
config-version: 2

profile: nba_shot_analytics

model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
test-paths: ["tests"]

vars:
  season: "2024-25"

models:
  nba_shot_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: ephemeral
    marts:
      +materialized: table
      +schema: analytics
